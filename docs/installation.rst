============
Installation
============

At the command line::

    $ easy_install django-cae

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-cae
    $ pip install django-cae
